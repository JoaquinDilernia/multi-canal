[build]
builder = "NIXPACKS"
buildCommand = "(cd ../dashboard && npm install && npm run build) && npm install && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
